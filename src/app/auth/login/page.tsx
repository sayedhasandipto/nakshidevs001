import Login from '@/components/auth/Login';

export const metadata = {
  title: 'Log In - ServiceHub',
  description: 'Log in to your ServiceHub account',
};

export default function LoginPage() {
  return <Login />;
}
